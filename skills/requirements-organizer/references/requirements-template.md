# 需求输出模板

按下面模板整理结果。可以根据模块实际情况删减无关小节，但不要省略“需求范围”“详细需求”“验收标准”“待确认问题”。
如果材料明显不足，再补一个“信息缺口”小节。

## 标注规则

- 用 `[明确]` 标识原型、标注、文案里已经明确给出的结论
- 用 `[推断]` 标识基于页面结构、交互路径或常见产品模式推导出的结论
- 用 `[待确认]` 标识证据不足、需要补充确认的内容

如果同时使用原型、代码或外部系统证据，可以追加来源标签，例如 `[明确][原型]`、`[明确][代码现状]`、`[明确][外部系统]`、`[推断][代码关联]`、`[推断][外部边界]`、`[待确认][无代码锚点]`、`[冲突]`。来源标签只用于关键结论，不要求每一句都标。

## 推荐模板

### 小需求开发版

适用于一个小功能点，优先给开发直接看。

```md
# <功能名>

## 1. 功能概述
- 做什么：
- 入口：

## 2. 交互与表现
- [明确] ...
- [明确] ...

## 3. 规则
- [明确] ...
- [推断] ...

## 4. 异常/边界
- [待确认] ...

## 5. 待确认问题
- ...
```

小需求默认优先使用这个模板，不强制展开完整 PRD 结构。
额外限制：

- “待确认问题”默认最多 3 条
- 只能写阻塞开发实现、联调或验收的问题
- 没有证据的按钮、字段、状态，不要写进正文，也不要写进待确认

```md
# <模块名称>需求文档

## 1. 需求范围
- 分析对象：
- 所属页面：
- 本次覆盖：
- 本次不覆盖：

## 2. 来源与依据
- 链接：
- 截图：
- 其他说明：
- 信息完整度：高 / 中 / 低
- 冲突说明：

### 2.1 页面元素核对
用于列表、表单、卡片、弹窗、详情、配置、流程页或历史漏采风险较高的页面。小需求可并入“交互与表现”，无需单独保留本节。

| 类别 | 已识别元素 | 处理结果 | 说明 |
| --- | --- | --- | --- |
| 页面区域 | 标题 / 页签 / 分组 / 入口 | 写入需求 / 不在范围 / 待确认 |  |
| 列表或卡片 | 主标题 / 列字段 / 行内元信息 / 标签 / 统计值 / 操作区 | 写入需求 / 不在范围 / 待确认 |  |
| 筛选与分页 | 搜索 / 筛选 / 排序 / 分页 / 加载更多 / 默认条件 | 写入需求 / 不在范围 / 待确认 |  |
| 表单与配置 | 输入 / 选择 / 开关 / 上传 / 默认值 / 必填 / 提示 | 写入需求 / 不在范围 / 待确认 |  |
| 动作与权限 | 按钮 / 链接 / 菜单 / 批量动作 / 禁用态 / 可见范围 | 写入需求 / 不在范围 / 待确认 |  |
| 状态与反馈 | 状态标签 / 空态 / 加载 / 处理中 / 成功 / 失败 / 忽略 / Toast / 二次确认 | 写入需求 / 不在范围 / 待确认 |  |
| 跳转与回流 | 进入来源 / 点击去向 / 提交回流 / 取消返回 / 重复进入 | 写入需求 / 不在范围 / 待确认 |  |

### 2.2 增量采集判断
只在重复采集、按最新原型更新、或同一项目多次采集时保留。

| 类别 | 内容 | 处理 |
| --- | --- | --- |
| 复用材料 |  |  |
| 跳过动作 |  |  |
| 补采缺口 |  |  |
| 本次写入范围 |  |  |

### 2.3 增量复盘
只在补需求、复测需求、解释“新增了什么”或追问“为什么之前漏掉”时保留。

| 问题 | 说明 |
| --- | --- |
| 本轮新增或修正 | 新增/修正的页面元素、字段、状态、动作、流程、接口或代码关联 |
| 上一版漏掉原因 | 未做可见元素清点 / 未做代码对照 / 只读局部截图 / 复用过期旧文档 / 只按用户线索补窄 / 漏读连线 / 误判外部边界等 |
| 本轮补齐的关联 | 对应需求条目、数据项、代码/文档锚点、仓库角色或信息缺口 |
| 仍未解决 | 没有原型证据 / 没有代码锚点 / 外部系统未展开 / 产品口径待确认 / 需要真实环境验证 |

## 3. 需求概述
- 业务目标：
- 用户价值：
- 触发场景：

## 4. 角色与前置条件
- 角色：
- 前置条件：
- 权限或可见范围：

## 5. 交互流程
1. ...
2. ...
3. ...

### 5.1 流程图节点与连线
只在原型包含流程图、状态图、脑图、页面连线或复杂节点关系时保留。

| 节点 Key | 节点 | 状态/颜色/图例 | 分组/泳道 | 上游 | 下游 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

| 连线 Key | 起点 | 终点 | 方向 | 标签/颜色/样式 | 置信度 | 待确认 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  | 高 / 中 / 低 |  |

### 5.2 变化对比
只在有旧截图、旧补充文档或轻量基线时保留。没有正式基线时，只能写“基于当前已采集材料未发现变化”，不要断言“原型无变化”。

| 变化项 | 旧情况 | 新情况 | 影响 | 处理 |
| --- | --- | --- | --- | --- |
| 节点新增/删除 |  |  |  |  |
| 节点状态/备注变化 |  |  |  |  |
| 连线新增/删除 |  |  |  |  |
| 连线方向/标签变化 |  |  |  |  |
| 模块归属变化 |  |  |  |  |

## 6. 详细需求

### 6.1 页面结构与信息展示
- [明确] ...
- [推断] ...

### 6.2 用户操作与交互规则
- [明确] ...
- [推断] ...

### 6.3 状态、反馈与提示
- [明确] 默认状态：
- [明确] 成功反馈：
- [待确认] 失败反馈：

### 6.4 校验、限制与业务规则
- [明确] ...
- [推断] ...

### 6.5 数据项
| 数据项 | 页面元素或业务含义 | 来源/去向 | 代码或文档锚点 | 是否必填 | 备注 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 6.6 代码关联与实现边界
只在用户要求核验代码关联，或原型、代码、外部系统需要对照时保留。如果当前需求有关联项目且仓库内存在相关代码，本节必须保留。

| 需求点 | 对应页面元素 | 仓库角色 | 代码锚点/外部边界 | 判断 |
| --- | --- | --- | --- | --- |
|  |  | 用户入口 / 前后端契约 / 包装层或适配层 / 异步承接 / 读侧查询 / 外部系统主体 / 新需求 / 待确认 / 无代码锚点 |  | 已关联 / 包装层 / 外部边界 / 异步承接 / 新需求 / 待确认 |

代码对照最少覆盖这些类别；没有命中时写缺口，不要删掉需求：

| 对照类别 | 需要核对 | 结果 |
| --- | --- | --- |
| 页面动作 | 入口 / 按钮 / 跳转 / 提交 / 回流 | 已有关联 / 新需求 / 待确认 |
| 展示数据 | 列表 / 卡片 / 表单 / 筛选 / 分页 / 统计 / 预览 | 已有关联 / 字段缺口 / 口径冲突 |
| 状态来源 | 状态标签 / 操作可用态 / 异常 / 处理中 / 成功失败 | 持久化字段 / 异步回流 / 外部状态 / 无代码锚点 |
| 跨服务能力 | 上传 / OCR / AI / 报告 / 打印 / 补扫 / 通知 | 包装层 / 消费者 / 外部系统 / 新增接口 |
| 旧结论复核 | 未实现 / 未发现 / 待确认 / 当前没有 | 仍成立 / 已过期 / 证据不足 |

### 6.7 旧结论复核
只在复用旧需求文档，且旧文档包含“未发现、未接入、当前没有、尚未、待确认、本轮未看到”等结论时保留。

| 旧结论 | 复核关键词/锚点 | 当前判断 | 说明 |
| --- | --- | --- | --- |
|  |  | 仍成立 / 已过期 / 证据不足 |  |

## 7. 异常与边界情况
- [待确认] ...

## 8. 信息缺口
- 当前无法确认但会影响实现或验收的内容：

## 9. 验收标准
- [明确] 当...时，应...
- [推断] 当...时，应...

## 10. 待确认问题
- ...

## 11. 证据追溯
只保留关键结论和容易漏采、易冲突的元素，不要把每一句话都做成追溯表。

| 结论或元素 | 类型 | 证据来源 | 代码/文档锚点 | 说明 |
| --- | --- | --- | --- | --- |
|  | 明确/推断/待确认/冲突 | 链接/截图/标注/文案/代码/旧文档 |  |  |
```

## 补充要求

- “详细需求”要写成可以交付设计、研发、测试继续使用的表达，不要只写“这里有一个按钮”
- “验收标准”要尽量可测试、可判定，避免空泛描述
- 如果信息不足，宁可保留 `[待确认]`，也不要自行杜撰
- 如果用户要求“研发/测试可落地版本”，优先把状态、校验、异常、回流和数据项写完整
- 如果是模块型需求，避免把整页无关区域也扩写进去
- 如果用户要求核验代码关联，只列关键需求点的锚点和边界；不要把每句需求都做成庞大追溯表
