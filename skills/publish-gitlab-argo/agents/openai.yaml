interface:
  display_name: "GitLab Argo 发布"
  short_description: "按本机 local config 计算 release tag、复用在途发布，并通过 GitLab 与 Argo CD API 发布配置中的应用。"
  default_prompt: "Use $publish-gitlab-argo, run the unified Python entry point, calculate or reuse the configured release tag, avoid duplicate in-flight publishes, and publish the configured development apps through GitLab and Argo CD APIs."
