# Codex 测试与验证规则

- 用户要求测试但未说明环境时，先确认使用本地、测试环境还是其他环境；用户已明确环境时直接按指定环境执行。
- 只读查询、静态检查和无副作用验证可以直接执行；写入、发布、删除、迁移和真实数据修改必须先确认。
- 真实数据写入测试前，先准备可执行回滚方案；执行前展示影响范围和回滚方式。
- 测试结论必须说明验证命令、验证对象、实际结果和未覆盖风险。
- 构建或测试失败时，先区分环境问题、依赖问题、权限问题和代码问题，不把它们混成一个结论。
- 本地服务验证时，先确认服务实际监听地址和端口，再发请求；不要凭历史端口试探。
- 平台差异命令不要写进公共规则；Windows 命令放入 Windows 配置，Mac 命令放入 Mac 配置。
- 测试过程中产生的日志、截图、缓存和临时文件默认不进入同步仓库。
